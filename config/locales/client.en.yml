en:
  js:
    category:
      discourse_activity_pub:
        settings_heading: ActivityPub
        enable: Enable ActivityPub in this category.
        enable_description: Site settings 'activity pub enabled' and 'login required' also affect whether ActivityPub is active.
        show_status: Show the ActivityPub status and visibility in this category's composer.
        show_status_description: Shows in the composer when creating a post that will be published with ActivityPub.
        show_handle: Show ActivityPub follow button in this category's topic list.
        show_handle_description: Shows a follow button to the left of the "New Topic" button.
        username: ActivityPub username for this category.
        username_description: Cannot be changed once saved.
        handle: ActivityPub handle for this category.
        handle_description: Used to follow this category on services using Webfinger (e.g. Mastodon).
        name: ActivityPub name for this category.
        name_description: Used as a display name on some services (e.g. Mastodon).
        default_visibility: ActivityPub visibility for this category.
        default_visibility_description: All posts will be published via ActivityPub with this visibility.
        post_object_type: ActivityPub type to publish posts in this category as.
        post_object_type_description: Use Note for short content (e.g. Mastodon) and Article for long content.
        publication_type: ActivityPub publication type.
        publication_type_description: First Post publishes the first post in each topic. Full Topic publishes all posts and ingests replies.
    discourse_activity_pub:
      status:
        title:
          login_required_enabled: ActivityPub does not work with login required
          plugin_disabled: The ActivityPub plugin is disabled
          category_read_restricted: ActivityPub does not work with private categories
          model_disabled: AcivityPub is disabled for this %{model_type}
          model_not_ready: ActivityPub is not ready for this %{model_type}
          model_not_active: ActivityPub is not active for this %{model_type}
          model_active:
            first_post: The first post of a new topic in %{category_name} will be published %{delay_minutes} minutes after being posted
        label:
          active: Active
          not_active: Not Active
      category_nav:
        label: Federation
        description: Federation for %{category_name}
      banner:
        intro: "This category's topics can be followed by users on ActivityPub social media like Mastodon. "
        public_full_topic: Each post of each topic will be published publicly.
        public_first_post: The first post of each topic will be published publicly.
        followers_only_full_topic: Each post of each topic is published to ActivityPub followers only.
        followers_only_first_post: The first post of each topic is published to ActivityPub followers only.
      handle:
        label: Other
        description: Search for this handle on a service that supports ActivityPub.
      follow:
        label: Follow
        title: Follow %{name} via ActivityPub
        domain:
          label: Mastodon
          placeholder: domain
          btn_label: Follow
          btn_title: Follow on this Mastodon domain
          description: domain of your Mastodon account
          verifying: verifying the domain...
          invalid: not a valid Mastodon domain
      follower:
        actor: Actor
        user: User
        followed_at: Followed
      visibility:
        label:
          private: Followers Only
          public: Public
        description:
          private: "%{object_type} is addressed to followers."
          public: "%{object_type} is publicly addressed."
      post_object_type:
        label:
          note: Note
          article: Article
        description:
          note: Best for microblogging platforms (e.g. Mastodon).
          article: Best for longform platforms (e.g. Lemmy).
      publication_type:
        label:
          first_post: First Post
          full_topic: Full Topic
        description:
          first_post: First post of every topic in this category is published via ActivityPub.
          full_topic: Every post of every topic in this category is published via ActivityPub and replies on other ActivityPub servers are imported as replies in Discourse.
      tip:
        status:
          description: You can follow this category on any social network that supports ActivityPub, such as Mastodon.

    post:
      discourse_activity_pub:
        title:
          published: "%{object_type} was published via ActivityPub from this site at %{time}."
          scheduled: "%{object_type} is scheduled to be published via ActivityPub from this site at %{time}."
          scheduled_past: "%{object_type} was scheduled to be published via ActivityPub from this site at %{time}."
          deleted: "%{object_type} was deleted via ActivityPub at %{time}."
          updated: "%{object_type} was updated via ActivityPub at %{time}."
          published_remote: "%{object_type} was published via ActivityPub from %{domain} at %{time}."
        info:
          title: "ActivityPub for Post #%{post_number}"
          url: Original %{object_type} on %{domain}.
        schedule:
          label: Schedule Publish
          title: Schedule post to be published via ActivityPub.
        unschedule:
          label: Unschedule Publish
          title: Unschedule post to be published via ActivityPub.
    user:
      discourse_activity_pub:
        title: ActivityPub
        authorize:
          title: Authorize
          description: Authorize an account on another domain to associate its activities with your user.
          placeholder: Enter a domain
          instructions: "e.g. mastodon.social"
        clear_domain_button:
          title: Clear domain.
        verify_domain_button:
          label: Verify
          title: Verify the domain where you will authorize an account.
        authorize_button:
          label: Authorize
          title: Authorize an account on the domain.
        authorizations:
          title: Authorizations
          description: Activities of these accounts will be associated with your user.
        authorization:
          confirm_remove: Are you sure you want to remove the authorization for %{actor_id}?
